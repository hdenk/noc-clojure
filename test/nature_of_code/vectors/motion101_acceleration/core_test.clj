(ns nature-of-code.vectors.motion101-acceleration.core-test
  (:use clojure.test)
  (:require [nature-of-code.vectors.motion101-acceleration.core :as motion101]))

; ein Dummy-Test, damit im output feedback zu sehen ist 
(deftest test-that-succeeds
    (testing 
      "succeed"
      (is 
        (= 1 1)))) 
