(ns nature-of-code.vectors.bouncingball-vectors.core-test
  (:use clojure.test)
  (:require [nature-of-code.vectors.bouncingball-vectors.core :as bouncingball-vectors]))

; besser als gar nix  
(deftest dummy
  (is true))
