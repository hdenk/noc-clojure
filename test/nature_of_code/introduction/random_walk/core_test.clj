(ns nature-of-code.introduction.random-walk.core-test
  (:use clojure.test)
  (:require [nature-of-code.introduction.random-walk.core :as random-walk]))

; besser als gar nix  
(deftest dummy
  (is true))
