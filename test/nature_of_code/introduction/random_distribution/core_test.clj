(ns nature-of-code.introduction.random-distribution.core-test
  (:use clojure.test)
  (:require [nature-of-code.introduction.random-distribution.core :as random-distribution]))

; besser als gar nix  
(deftest dummy
  (is true))
